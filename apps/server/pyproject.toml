[project]
name = "hermes-voice-control-server"
version = "0.1.0"
description = "FastAPI backend for Hermes Voice Control"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.116.0", "uvicorn[standard]>=0.35.0", "pydantic>=2.8.0"]

[project.optional-dependencies]
dev = ["pytest>=8.3.0", "httpx>=0.27.0"]
real-gemini = ["google-genai>=1.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
